name: TDS GA7 Release Gate

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "TDS identity: 22f3002249@ds.study.iitm.ac.in"
        run: echo "Identity verified"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run verification test
        run: echo "Workflow executed successfully on main branch"
