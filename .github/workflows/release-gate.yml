name: TDS GA7 Release Gate

on:
  push:
    branches:
      - main

jobs:
  test-release-gate:
    runs-on: ubuntu-latest
    steps:
      - name: "TDS identity: 22f3002249@ds.study.iitm.ac.in"
        run: echo "Identity verified"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Test Release Gate Logic
        run: |
          python -c '
          import main
          print("Release gate logic loaded successfully")
          '
